apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cluster-reader-role
rules:
- apiGroups: ["", "apps"]
  resources:
    - pods
    - deployments
    - services
    - namespaces
  verbs:
    - get
    - list
    - watch
